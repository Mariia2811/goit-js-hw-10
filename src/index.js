import './css/styles.css';
import fetchCountries from './fetchCountries';

const DEBOUNCE_DELAY = 300;

const refFormInput = document.querySelector('#search-box');
refFormInput.addEventListener('input', e => {
    
})

fetchCountries(name);

